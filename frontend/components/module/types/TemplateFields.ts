export type TemplateFields = {
  name: string;
  description: string;
  imageURL: string;
  moduleType: string;
  subscriptionType: string;
  systemTemplate: boolean;
  id: string;
  addedByUserId: string;
};
